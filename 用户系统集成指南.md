# 用户系统集成指南

## 已完成的工作

### 1. 数据模型和数据库
- ✅ 创建了 `User.kt` 用户数据模型
- ✅ 创建了 `UserDao.kt` 数据访问对象
- ✅ 创建了 `UserSessionManager.kt` 会话管理器
- ✅ 创建了 `UserRepository.kt` 用户仓库
- ✅ 创建了 `AuthViewModel.kt` 认证视图模型
- ✅ 更新了 `AppDatabase.kt` 添加 User 表（版本12）

### 2. UI 页面
- ✅ 创建了 `WelcomeScreen.kt` 欢迎页面
- ✅ 创建了 `LoginScreen.kt` 登录页面（可爱风格）
- ✅ 创建了 `RegisterScreen.kt` 注册页面（可爱风格，内测码：223498）

## 需要手动完成的步骤

### 步骤1: 更新导航图

在 `NavGraph.kt` 中添加新的路由：

```kotlin
// 在文件顶部添加路由常量
object Routes {
    const val WELCOME = "welcome"
    const val LOGIN = "login"
    const val REGISTER = "register"
    const val HOME = "home"
    // ... 其他现有路由
}

// 在 NavGraph composable 中添加：
@Composable
fun NavGraph(
    navController: NavHostController,
    authViewModel: AuthViewModel // 添加这个参数
) {
    NavHost(
        navController = navController,
        startDestination = Routes.WELCOME // 改为从欢迎页开始
    ) {
        // 欢迎页
        composable(Routes.WELCOME) {
            WelcomeScreen(
                viewModel = authViewModel,
                onNavigateToLogin = { navController.navigate(Routes.LOGIN) },
                onNavigateToRegister = { navController.navigate(Routes.REGISTER) },
                onNavigateToHome = {
                    navController.navigate(Routes.HOME) {
                        popUpTo(Routes.WELCOME) { inclusive = true }
                    }
                }
            )
        }
        
        // 登录页
        composable(Routes.LOGIN) {
            LoginScreen(
                viewModel = authViewModel,
                onLoginSuccess = {
                    navController.navigate(Routes.HOME) {
                        popUpTo(Routes.WELCOME) { inclusive = true }
                    }
                },
                onNavigateToRegister = {
                    navController.navigate(Routes.REGISTER) {
                        popUpTo(Routes.LOGIN) { inclusive = true }
                    }
                }
            )
        }
        
        // 注册页
        composable(Routes.REGISTER) {
            RegisterScreen(
                viewModel = authViewModel,
                onRegisterSuccess = {
                    navController.navigate(Routes.HOME) {
                        popUpTo(Routes.WELCOME) { inclusive = true }
                    }
                },
                onNavigateToLogin = {
                    navController.navigate(Routes.LOGIN) {
                        popUpTo(Routes.REGISTER) { inclusive = true }
                    }
                }
            )
        }
        
        // ... 其他现有路由
    }
}
```

### 步骤2: 更新 MainActivity

在 `MainActivity.kt` 中创建 AuthViewModel 实例：

```kotlin
class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
            FunLifeTheme {
                val navController = rememberNavController()
                val authViewModel: AuthViewModel = viewModel() // 添加这行
                
                NavGraph(
                    navController = navController,
                    authViewModel = authViewModel // 传递给NavGraph
                )
            }
        }
    }
}
```

### 步骤3: 添加登出功能

在 `HomeScreen.kt` 或设置页面中添加登出按钮：

```kotlin
// 在 HomeScreen 或 SettingsScreen 中
val authViewModel: AuthViewModel = viewModel()

// 添加登出按钮
IconButton(onClick = {
    authViewModel.logout()
    // 导航回欢迎页
    navController.navigate(Routes.WELCOME) {
        popUpTo(0) { inclusive = true }
    }
}) {
    Icon(Icons.Default.Logout, "登出")
}
```

### 步骤4: 获取当前用户ID

在需要用户ID的地方（如保存数据时）：

```kotlin
val authViewModel: AuthViewModel = viewModel()
val session = authViewModel.getCurrentSession()
val userId = session?.userId ?: -1L

// 使用 userId 保存数据
```

## 未来改进（可选）

### 1. 为现有数据表添加 userId 字段

这需要数据库迁移，建议在测试环境中进行：

```kotlin
// 示例：为 Anniversary 表添加 userId
@Entity(tableName = "anniversaries")
data class Anniversary(
    @PrimaryKey(autoGenerate = true)
    val id: Long = 0,
    val userId: Long = 0, // 添加这个字段
    // ... 其他字段
)

// 在 DAO 中添加按用户查询
@Query("SELECT * FROM anniversaries WHERE userId = :userId")
fun getAnniversariesByUser(userId: Long): Flow<List<Anniversary>>
```

### 2. 密码加密

目前密码是明文存储，生产环境应该加密：

```kotlin
// 使用 BCrypt 或其他加密库
import org.mindrot.jbcrypt.BCrypt

fun hashPassword(password: String): String {
    return BCrypt.hashpw(password, BCrypt.gensalt())
}

fun checkPassword(password: String, hashed: String): Boolean {
    return BCrypt.checkpw(password, hashed)
}
```

### 3. 记住登录状态

已通过 `UserSessionManager` 实现，应用重启后会自动检查登录状态。

### 4. 用户头像

可以添加头像选择功能：
- 使用预设头像（表情符号）
- 从相册选择
- 拍照上传

## 测试步骤

1. 编译并运行应用
2. 应该看到欢迎页面
3. 点击"注册"，输入：
   - 用户名：test（至少3个字符）
   - 昵称：测试用户（可选）
   - 密码：123456（至少6个字符）
   - 确认密码：123456
   - 内测码：223498
4. 注册成功后自动登录并进入主页
5. 重启应用，应该直接进入主页（保持登录状态）
6. 点击登出，返回欢迎页

## 注意事项

- 数据库版本已从11升级到12
- 首次运行会重建数据库（因为使用了 fallbackToDestructiveMigration）
- 现有数据会丢失，建议先备份
- 内测码硬编码为：223498

## 文件清单

新增文件：
1. `data/model/User.kt`
2. `data/dao/UserDao.kt`
3. `repository/UserRepository.kt`
4. `viewmodel/AuthViewModel.kt`
5. `utils/UserSessionManager.kt`
6. `ui/screens/WelcomeScreen.kt`
7. `ui/screens/LoginScreen.kt`
8. `ui/screens/RegisterScreen.kt`

修改文件：
1. `data/database/AppDatabase.kt` - 添加 User 表，版本升级到12
