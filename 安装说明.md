# 安装到手机

## 核心修复说明

这次修复采用了全新的数据流架构，彻底解决纪念日第一次添加不显示的问题：

### 关键改进
1. **放弃复杂的Flow组合** - 不再使用 `combine` + `stateIn`，改用 `MutableStateFlow` 直接管理
2. **主动加载机制** - 添加 `loadAnniversaries()` 方法，每次数据变化后主动重新加载
3. **确保数据同步** - 在IO线程执行数据库操作，等待300ms确保事务提交，再等待200ms确保UI更新
4. **详细日志输出** - 每一步都有日志，方便追踪问题

### 数据流程
```
添加纪念日 → 数据库插入(IO线程) → 等待300ms → 主动调用loadAnniversaries() → 
从数据库查询最新数据 → 更新MutableStateFlow → 等待200ms → UI自动更新
```

## 安装步骤

### 方法1：使用gradlew命令
```bash
./gradlew installDebug
```

### 方法2：手动拖拽APK
APK文件位置：`app/build/outputs/apk/debug/app-debug.apk`

直接拖拽到手机即可安装

## 测试步骤

1. **清理旧数据**（重要！）
   - 在手机上卸载旧版本应用
   - 或在设置中清除应用数据

2. **安装新版本**

3. **注册新账号**
   - 测试输入法是否正常弹出

4. **添加第一个纪念日**
   - 填写名称、日期
   - 点击保存
   - **关键**：观察是否立即显示在列表中

5. **查看日志**（如果还有问题）
   - 在Android Studio的Logcat中
   - 过滤：`AnniversaryViewModel`
   - 应该看到完整的添加流程日志

## 预期日志输出

```
=== 开始添加纪念日 ===
name: 测试, date: 2024-01-01, type: CUSTOM
userId: 1
准备插入数据库: Anniversary(...)
✅ 数据库插入完成
主动重新加载数据...
=== 加载纪念日数据 ===
userId: 1
从数据库获取到 1 条纪念日
过滤排序后 1 条
  - 测试 (ID: 1, userId: 1)
✅ 纪念日添加成功，当前列表数量: 1
```

## 如果还有问题

请提供Logcat日志，特别是：
- `AnniversaryViewModel` 标签的所有日志
- 是否看到"从数据库获取到 X 条纪念日"
- 当前列表数量是多少

这次的修复是架构级别的改进，应该能彻底解决问题！
