# 启动页面修复说明

## 问题
手机上只能看到第二个启动页面（黄色背景的"趣味生活"），看不到第一个启动页面（白色背景的小狗动画）。

## 原因
1. 系统启动画面（Android 12+ Splash Screen API）显示时间太短（原来是1秒）
2. 缺少 Splash Screen 库依赖
3. 没有使用 `setKeepOnScreenCondition` 来延长显示时间

## 修复内容

### 1. 延长系统启动画面显示时间
**文件**: `app/src/main/res/values/themes.xml`
- 将 `android:windowSplashScreenAnimationDuration` 从 1000ms 改为 2000ms

### 2. 添加 Splash Screen 库依赖
**文件**: `app/build.gradle.kts`
- 添加: `implementation("androidx.core:core-splashscreen:1.0.1")`

### 3. 在 MainActivity 中控制显示时间
**文件**: `app/src/main/java/com/example/funlife/MainActivity.kt`
- 使用 `installSplashScreen()` API
- 使用 `setKeepOnScreenCondition` 保持启动画面显示2秒
- 添加必要的导入：
  - `androidx.core.splashscreen.SplashScreen.Companion.installSplashScreen`
  - `androidx.lifecycle.lifecycleScope`
  - `kotlinx.coroutines.delay`
  - `kotlinx.coroutines.launch`

## 启动流程
现在应用启动时会显示两个启动页面：

1. **系统启动画面**（2秒）
   - 白色背景
   - 小狗动画（耳朵摇摆、鼻子呼吸、项圈闪烁）
   - 由 Android 系统管理

2. **应用启动页面**（3秒）
   - 黄色渐变背景
   - "趣味生活" 文字
   - 进度条动画
   - 由 SplashScreen.kt 管理

## 测试步骤
1. 清理并重新编译：`gradlew.bat clean assembleDebug`
2. 卸载旧版本：`adb uninstall com.example.funlife`
3. 安装新版本：`adb install app\build\outputs\apk\debug\app-debug.apk`
4. 启动应用，观察是否能看到两个启动页面

## 注意事项
- 系统启动画面只在 Android 12 (API 31) 及以上版本显示
- 如果手机是 Android 11 或更低版本，只会看到第二个启动页面
- 在模拟器上测试时，确保使用 Android 12+ 的系统镜像
