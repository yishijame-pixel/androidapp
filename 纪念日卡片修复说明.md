# 纪念日卡片修复说明

## 已修复的问题

### 1. 卡片背景图片完全覆盖 ✅
- **问题**: 卡片底部有空白，背景图片没有完全覆盖
- **解决方案**: 
  - 使用 `matchParentSize()` 替代 `fillMaxHeight()`，让背景图片完全匹配父容器大小
  - 设置卡片最小高度 `heightIn(min = 280.dp)` 确保有足够空间显示内容
  - 使用 `ContentScale.Crop` 裁剪图片以填充整个区域

### 2. 操作按钮展开/收起功能 ✅
- **问题**: 操作按钮（编辑、分享、删除）需要可以通过箭头展开/收起
- **解决方案**:
  - 添加 `showActions` 状态变量控制按钮显示
  - 添加箭头按钮（向上/向下）用于切换状态
  - 使用 `if (showActions)` 条件渲染操作按钮行
  - 默认状态为收起，用户点击箭头后展开

### 3. 图片永久保存 ✅
- **问题**: 用户更换的图片没有永久保存，每次重新编译都恢复原样
- **原因**: 之前直接保存 `content://` URI，这是临时URI，应用重启后会失效
- **解决方案**:
  - 创建 `ImageHelper.kt` 工具类
  - 在添加/编辑纪念日时，将图片从临时URI复制到应用私有目录 (`/data/data/com.example.funlife/files/anniversary_images/`)
  - 保存 `file://` URI 到数据库，这是永久路径
  - 删除纪念日时自动清理关联的图片文件
  - 更新图片时删除旧图片，避免占用空间

## 技术实现

### ImageHelper 工具类
```kotlin
// 复制图片到应用私有目录
fun copyImageToAppStorage(context: Context, sourceUri: Uri): String?

// 删除图片文件
fun deleteImage(imageUri: String?)

// 清理未使用的图片
fun cleanupUnusedImages(context: Context, usedImageUris: List<String>)
```

### AnniversaryViewModel 更新
- `addAnniversary()`: 添加时复制图片到应用目录
- `updateAnniversary()`: 更新时处理新旧图片，删除旧图片
- `deleteAnniversary()`: 删除时清理关联图片

### AnniversaryCard 设计改进
- 背景图片使用 `matchParentSize()` 完全覆盖
- 渐变遮罩从透明到半透明，确保文字可读
- 操作按钮默认隐藏，点击箭头展开
- 卡片最小高度280dp，确保内容完整显示

## 使用说明

1. **添加纪念日**: 选择图片后，图片会自动复制到应用私有目录，永久保存
2. **编辑纪念日**: 更换图片时，旧图片会被删除，新图片会被保存
3. **删除纪念日**: 删除纪念日时，关联的图片也会被自动删除
4. **操作按钮**: 点击卡片底部的箭头按钮可以展开/收起编辑、分享、删除按钮

## 测试建议

1. 添加一个带图片的纪念日
2. 关闭应用并重新打开，确认图片仍然显示
3. 编辑纪念日并更换图片，确认新图片正确显示
4. 重新编译应用，确认图片不会丢失
5. 测试操作按钮的展开/收起功能
6. 确认背景图片完全覆盖卡片，没有底部空白

## 注意事项

- 图片保存在应用私有目录，卸载应用时会被删除
- 图片文件名使用UUID生成，避免冲突
- 支持自动清理未使用的图片（可选功能）
- 图片格式统一保存为 `.jpg`
