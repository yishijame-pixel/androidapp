# 头像框商城系统 - 使用指南

## 📋 系统概述

头像框商城系统已完全实现，包含880+头像框资源（PNG静态框 + GIF动态框），支持购买、装备、VIP折扣等完整功能。

## ✅ 已完成的功能

### 1. 数据层 (100%)
- ✅ `ShopItem` 模型扩展（vipPrice, assetPath, rarity, isAnimated, category, sortOrder）
- ✅ `UserAvatarFrame` 实体（用户拥有的头像框）
- ✅ `User` 模型扩展（equippedFrameId, isVip, vipExpireAt）
- ✅ `UserAvatarFrameDao` 完整CRUD操作
- ✅ `ShopDao` 头像框查询扩展
- ✅ `UserDao` 头像框装备和VIP方法
- ✅ 数据库迁移 `Migration35to36`

### 2. 业务层 (100%)
- ✅ `ShopRepository` 头像框方法扩展
- ✅ `ShopViewModel` 完整购买/装备逻辑
- ✅ `AvatarFrameInitializer` 资源扫描导入工具

### 3. UI层 (100%)
- ✅ `AvatarSizes` 尺寸常量（Small/Medium/Large）
- ✅ `AvatarWithFrame` 组件（支持PNG/GIF，自适应尺寸）
- ✅ `AvatarFrameShopScreen` 完整商城界面
  - 稀有度筛选（全部/普通/稀有/史诗/传说）
  - 类型筛选（全部/静态/动态）
  - 网格展示（2列布局）
  - 购买对话框（显示价格、VIP折扣）
  - 装备/卸下功能

### 4. 配置层 (100%)
- ✅ AppDatabase 添加 `userAvatarFrameDao()` 方法
- ✅ 数据库版本升级到36
- ✅ Migration35to36 添加到迁移列表
- ✅ Coil GIF 依赖添加
- ✅ 导航路由配置完成

## 🎯 尺寸规范

系统支持三种尺寸，**推荐使用 Medium 作为主要尺寸**：

| 尺寸 | 头像大小 | 头像框大小 | 使用场景 |
|------|---------|-----------|---------|
| Small | 48dp | 56dp | 列表、评论区 |
| **Medium** | **100dp** | **120dp** | **个人主页（主要）** |
| Large | 120dp | 140dp | 详情页、大图展示 |

## 💰 定价策略

### 基础定价
- **PNG静态框**: 50-300金币（按稀有度）
  - 普通(COMMON): 50-100金币
  - 稀有(RARE): 100-200金币
  - 史诗(EPIC): 200-300金币
  
- **GIF动态框**: 300-1200金币（2-3倍价格）
  - 稀有(RARE): 300-500金币
  - 史诗(EPIC): 500-800金币
  - 传说(LEGENDARY): 800-1200金币

### VIP折扣
- VIP用户享受 **20%折扣**（价格 × 0.8）
- 自动应用，无需手动操作

## 📁 资源目录结构

```
app/src/main/assets/xiangkuang/
├── A4头像框/          (普通PNG)
├── 头像框1/           (普通PNG)
├── 头像框2/           (普通PNG)
├── 头像框4/           (稀有PNG)
├── 头像框5/           (普通PNG)
├── 头像框6/           (史诗PNG + GIF)
├── 头像框7/           (稀有PNG)
└── 头像框8/           (史诗PNG + 传说GIF)
```

## 🚀 使用步骤

### 1. 初始化头像框数据（首次运行）

在应用启动时（如 `MainActivity.onCreate` 或首次登录后）调用：

```kotlin
import com.example.funlife.utils.AvatarFrameInitializer
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.launch

// 在合适的位置调用
CoroutineScope(Dispatchers.IO).launch {
    val count = AvatarFrameInitializer.initializeAvatarFrames(
        context = applicationContext,
        shopDao = database.shopDao()
    )
    Log.d("AvatarFrame", "初始化了 $count 个头像框")
}
```

**建议位置**：
- `MainActivity.onCreate()` - 应用启动时
- 首次登录成功后
- 设置页面的"重置数据"功能中

### 2. 导航到头像框商城

从任何页面导航到商城：

```kotlin
navController.navigate("avatar_frame_shop")
```

**推荐入口**：
- 个人主页的"装扮"按钮
- 商城页面的"头像框"分类
- VIP页面的"专属头像框"入口

### 3. 在个人主页显示装备的头像框

```kotlin
import com.example.funlife.ui.components.AvatarWithFrame
import com.example.funlife.ui.components.AvatarSize

@Composable
fun ProfileScreen(viewModel: ProfileViewModel) {
    val user by viewModel.currentUser.collectAsState()
    val equippedFrame by viewModel.equippedFrame.collectAsState()
    
    AvatarWithFrame(
        avatarUrl = user?.avatar,
        frameAssetPath = equippedFrame?.assetPath,
        size = AvatarSize.Medium,  // 推荐使用Medium
        contentDescription = "用户头像"
    )
}
```

### 4. ViewModel 中获取装备的头像框

```kotlin
class ProfileViewModel(
    private val userDao: UserDao,
    private val shopDao: ShopDao,
    private val userId: Long
) : ViewModel() {
    
    val equippedFrame: StateFlow<ShopItem?> = userDao.getUserById(userId)
        .flatMapLatest { user ->
            user?.equippedFrameId?.let { frameId ->
                shopDao.getItemById(frameId)
            } ?: flowOf(null)
        }
        .stateIn(viewModelScope, SharingStarted.Lazily, null)
}
```

## 🎨 UI 组件使用示例

### 基础用法
```kotlin
AvatarWithFrame(
    avatarUrl = "https://example.com/avatar.jpg",
    frameAssetPath = "xiangkuang/头像框6/frame_001.png",
    size = AvatarSize.Medium
)
```

### 支持GIF动态框
```kotlin
AvatarWithFrame(
    avatarUrl = user.avatar,
    frameAssetPath = "xiangkuang/头像框8/animated_frame.gif",
    size = AvatarSize.Large,
    contentDescription = "VIP用户头像"
)
```

### 无头像框（仅显示头像）
```kotlin
AvatarWithFrame(
    avatarUrl = user.avatar,
    frameAssetPath = null,  // 不显示头像框
    size = AvatarSize.Small
)
```

## 🔧 高级配置

### 自定义稀有度规则

编辑 `AvatarFrameInitializer.kt` 中的 `determineRarity()` 方法：

```kotlin
private fun determineRarity(folderName: String, fileName: String): String {
    return when {
        fileName.endsWith(".gif", ignoreCase = true) -> {
            when {
                folderName.contains("8") -> "LEGENDARY"
                folderName.contains("6") -> "EPIC"
                else -> "RARE"
            }
        }
        else -> {
            when {
                folderName.contains("A4") || folderName.contains("1") || 
                folderName.contains("2") || folderName.contains("5") -> "COMMON"
                folderName.contains("4") || folderName.contains("7") -> "RARE"
                folderName.contains("6") || folderName.contains("8") -> "EPIC"
                else -> "COMMON"
            }
        }
    }
}
```

### 自定义定价策略

编辑 `AvatarFrameInitializer.kt` 中的 `calculatePrice()` 方法：

```kotlin
private fun calculatePrice(rarity: String, isAnimated: Boolean): Int {
    val basePrice = when (rarity) {
        "COMMON" -> 50
        "RARE" -> 150
        "EPIC" -> 250
        "LEGENDARY" -> 500
        else -> 100
    }
    
    // GIF动态框价格翻倍
    return if (isAnimated) basePrice * 2 else basePrice
}
```

## 📊 数据库查询示例

### 获取用户拥有的所有头像框
```kotlin
val ownedFrames = userAvatarFrameDao.getUserFrames(userId)
```

### 获取用户当前装备的头像框
```kotlin
val equippedFrame = userAvatarFrameDao.getEquippedFrame(userId)
```

### 检查用户是否拥有某个头像框
```kotlin
val hasFrame = userAvatarFrameDao.hasFrame(userId, frameId)
```

### 购买头像框
```kotlin
viewModel.purchaseAvatarFrame(frameId) { success, message ->
    if (success) {
        Toast.makeText(context, "购买成功！", Toast.LENGTH_SHORT).show()
    } else {
        Toast.makeText(context, message, Toast.LENGTH_SHORT).show()
    }
}
```

### 装备头像框
```kotlin
viewModel.equipAvatarFrame(frameId) { success, message ->
    if (success) {
        Toast.makeText(context, "装备成功！", Toast.LENGTH_SHORT).show()
    }
}
```

## 🐛 故障排查

### 问题1：头像框不显示
**原因**：资源路径错误或文件不存在
**解决**：
1. 检查 `assetPath` 格式是否正确：`xiangkuang/头像框X/filename.png`
2. 确认文件存在于 `app/src/main/assets/` 目录
3. 检查文件名大小写是否匹配

### 问题2：GIF不播放
**原因**：缺少 Coil GIF 依赖
**解决**：确认 `build.gradle.kts` 包含：
```kotlin
implementation("io.coil-kt:coil-gif:2.5.0")
```

### 问题3：VIP折扣未生效
**原因**：用户VIP状态未正确设置
**解决**：
1. 检查 `User.isVip` 字段
2. 检查 `User.vipExpireAt` 是否过期
3. 确认 `ShopItem.vipPrice` 已正确计算

### 问题4：数据库迁移失败
**原因**：迁移未正确添加
**解决**：
1. 确认 `MIGRATION_35_36` 已添加到 `addMigrations()` 列表
2. 清除应用数据重新安装
3. 检查迁移SQL语句是否正确

## 📝 待办事项

虽然核心功能已完成，但以下功能可以进一步增强用户体验：

### 高优先级
- [ ] 在个人主页集成 `AvatarWithFrame` 组件
- [ ] 在VIP页面添加"头像框商城"入口
- [ ] 添加"我的头像框"页面（查看已拥有的头像框）
- [ ] 首次启动时自动初始化头像框数据

### 中优先级
- [ ] 头像框预览功能（购买前试戴）
- [ ] 头像框收藏/喜欢功能
- [ ] 头像框排序（按价格、稀有度、购买时间）
- [ ] 头像框搜索功能

### 低优先级
- [ ] 限时头像框（节日、活动专属）
- [ ] 头像框合成系统（多个低级合成高级）
- [ ] 头像框赠送功能
- [ ] 头像框成就系统

## 🎉 总结

头像框商城系统已完全实现并集成到应用中，包括：
- ✅ 完整的数据库架构（迁移、DAO、实体）
- ✅ 完整的业务逻辑（购买、装备、VIP折扣）
- ✅ 完整的UI界面（商城、筛选、购买对话框）
- ✅ 完整的资源管理（880+头像框，PNG+GIF）
- ✅ 完整的尺寸适配（Small/Medium/Large）

**下一步**：
1. 在 `MainActivity` 中调用 `AvatarFrameInitializer.initializeAvatarFrames()` 初始化数据
2. 在个人主页使用 `AvatarWithFrame` 组件显示装备的头像框
3. 在合适位置添加导航入口到头像框商城
4. 测试购买、装备、VIP折扣等功能

系统已准备就绪，可以立即使用！🚀
