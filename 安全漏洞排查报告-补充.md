# 🔒 FunLife 项目安全漏洞排查报告 - 补充篇

## 📅 排查日期
2026年3月16日

---

## 🆕 新发现的漏洞和问题

### 11. ⚠️ 数据库迁移使用 fallbackToDestructiveMigration
**严重程度**: 🟠 High  
**影响范围**: 所有用户数据

**问题描述**:
```kotlin
// AppDatabase.kt
Room.databaseBuilder(...)
    .addMigrations(...)
    .fallbackToDestructiveMigration()  // ← 危险！
    .build()
```

- 当数据库版本升级但缺少迁移脚本时，会直接删除所有数据
- 用户的所有纪念日、习惯、心情记录等会全部丢失
- 没有数据备份机制

**修复建议**:
1. 移除 `fallbackToDestructiveMigration()`
2. 为每个版本升级编写完整的迁移脚本
3. 添加数据备份功能
4. 在升级前提示用户备份数据

```kotlin
// 修复后
Room.databaseBuilder(...)
    .addMigrations(
        MIGRATION_1_2, 
        MIGRATION_2_3,
        // ... 所有迁移脚本
        MIGRATION_11_12  // 确保每个版本都有迁移
    )
    // 移除 fallbackToDestructiveMigration()
    .build()
```

---

### 12. ⚠️ 内测码硬编码在客户端
**严重程度**: 🟡 Medium  
**影响范围**: 注册安全

**问题描述**:
```kotlin
// AuthViewModel.kt:80
if (betaCode != "223498") {
    _authState.value = AuthState.Error("内测码错误")
    return@launch
}
```

- 内测码直接硬编码在APK中
- 反编译APK即可获取内测码
- 无法动态更新内测码

**修复建议**:
1. 将内测码验证移至服务端
2. 或使用加密存储 + 混淆
3. 支持多个内测码和过期时间

---

### 13. ⚠️ 习惯打卡金币奖励逻辑漏洞
**严重程度**: 🟡 Medium  
**影响范围**: 金币经济系统

**问题描述**:
```kotlin
// HabitViewModel.kt
// 用户可以通过以下方式刷金币：
// 1. 打卡 → 获得10金币
// 2. 立即取消打卡 → 返还10金币（但连续天数已增加）
// 3. 重复步骤1-2，可以无限刷连续天数
// 4. 达到7天倍数时获得50金币奖励
```

**实际测试**:
- 用户可以在同一天内反复打卡/取消打卡
- 每次打卡都会增加连续天数计数
- 可以快速达到7天倍数获得大量奖励

**修复建议**:
1. 取消打卡时重置连续天数
2. 限制每天只能打卡一次（不允许取消）
3. 或者取消打卡需要扣除更多金币作为惩罚

---

### 14. ⚠️ Flow 异常未捕获
**严重程度**: 🟡 Medium  
**影响范围**: 应用稳定性

**问题描述**:
- 大量使用 `collectAsState()` 但没有异常处理
- 如果数据库查询失败，会导致应用崩溃
- 没有全局异常处理机制

**受影响位置**:
```kotlin
// 所有 ViewModel 的 Flow
val habits: StateFlow<List<Habit>> = repository.allHabits.stateIn(...)
// 如果 repository.allHabits 抛出异常，应用会崩溃
```

**修复建议**:
```kotlin
val habits: StateFlow<List<Habit>> = repository.allHabits
    .catch { e ->
        Log.e("HabitViewModel", "Error loading habits", e)
        emit(emptyList())  // 发送空列表作为降级方案
    }
    .stateIn(...)
```

---

### 15. ⚠️ 补卡卡片可以无限使用
**严重程度**: 🔴 Critical  
**影响范围**: 习惯打卡系统

**问题描述**:
```kotlin
// HabitViewModel.kt:toggleCheckIn
// 补卡逻辑：
val success = repository.useMakeupCard(habitId)
if (success) {
    repository.checkIn(habitId, targetDate, ...)
    coinRepository.addCoins(5)  // 还奖励5金币！
}
```

**漏洞分析**:
1. 用户可以对同一天反复补卡
2. 每次补卡都消耗1张卡片，但也获得5金币
3. 如果用户有足够的补卡卡片，可以对同一天补卡多次
4. 每次补卡都增加总打卡天数

**修复建议**:
```kotlin
// 检查该日期是否已经打卡
val existingRecord = repository.getRecordByDate(habitId, targetDate)
if (existingRecord != null) {
    emit(CheckInResult.Failed("该日期已打卡"))
    return@flow
}
```

---

### 16. ⚠️ 用户会话永不过期
**严重程度**: 🟡 Medium  
**影响范围**: 账户安全

**问题描述**:
```kotlin
// UserSessionManager.kt
// 会话信息存储在 SharedPreferences 中
// 没有过期时间检查
// 用户登录一次后永久保持登录状态
```

**风险**:
- 设备丢失后，拾得者可以直接访问用户数据
- 没有自动登出机制
- 无法远程登出设备

**修复建议**:
```kotlin
data class UserSession(
    val userId: Long,
    val username: String,
    val nickname: String,
    val avatar: String,
    val loginTime: Long,  // 添加登录时间
    val expiryTime: Long  // 添加过期时间（如7天后）
)

fun isSessionValid(): Boolean {
    val session = getSession() ?: return false
    return System.currentTimeMillis() < session.expiryTime
}
```

---

### 17. ⚠️ 转盘历史记录无限增长
**严重程度**: 🟡 Medium  
**影响范围**: 性能和存储

**问题描述**:
- 每次转盘都会保存历史记录
- 没有自动清理机制
- 长期使用会导致数据库膨胀

**修复建议**:
```kotlin
// 定期清理旧记录（保留最近1000条）
suspend fun cleanOldHistory() {
    val count = historyDao.getCount()
    if (count > 1000) {
        historyDao.deleteOldest(count - 1000)
    }
}
```

---

### 18. ⚠️ 金币可以变成负数
**严重程度**: 🔴 Critical  
**影响范围**: 金币系统

**问题描述**:
```kotlin
// CoinDao.kt
@Query("UPDATE user_coins SET coins = coins - :amount WHERE id = 1")
suspend fun spendCoins(amount: Int)
```

- SQL 没有检查余额是否足够
- 如果并发扣除，可能导致负数金币
- 没有数据库约束防止负数

**修复建议**:
```kotlin
// 方案1：添加 CHECK 约束
@Entity(tableName = "user_coins")
data class UserCoins(
    @PrimaryKey val id: Long = 1,
    @ColumnInfo(name = "coins", defaultValue = "0")
    val coins: Int = 0,
    // 在数据库迁移中添加：
    // CHECK (coins >= 0)
)

// 方案2：在 SQL 中检查
@Query("""
    UPDATE user_coins 
    SET coins = coins - :amount 
    WHERE id = 1 AND coins >= :amount
""")
suspend fun spendCoinsAtomic(amount: Int): Int
```

---

### 19. ⚠️ 未实现的功能（TODO）
**严重程度**: 🟢 Low  
**影响范围**: 用户体验

**发现的 TODO**:
1. ProfileScreen.kt - 个人资料编辑（4处）
2. SettingsScreen.kt - 数据导入导出（2处）
3. SpinWheelScreen.kt - 音效和震动（3处）
4. EnhancedSpinWheelScreen.kt - 创建自定义模式（1处）

**建议**:
- 要么实现这些功能
- 要么隐藏这些入口，避免用户点击后无响应

---

### 20. ⚠️ 日期解析异常处理不一致
**严重程度**: 🟢 Low  
**影响范围**: 数据显示

**问题描述**:
```kotlin
// 多处使用 try-catch 捕获日期解析异常
try {
    LocalDate.parse(date)
} catch (e: Exception) {
    // 返回原始字符串
}
```

- 异常处理不一致
- 没有统一的日期格式验证
- 可能导致显示混乱

**修复建议**:
```kotlin
// 创建统一的日期工具类
object DateUtils {
    fun parseDate(dateStr: String): LocalDate? {
        return try {
            LocalDate.parse(dateStr)
        } catch (e: Exception) {
            Log.w("DateUtils", "Failed to parse date: $dateStr", e)
            null
        }
    }
    
    fun formatDate(date: LocalDate, pattern: String = "yyyy-MM-dd"): String {
        return date.format(DateTimeFormatter.ofPattern(pattern))
    }
}
```

---

## 📊 漏洞统计总结

### 按严重程度分类
- 🔴 Critical（严重）: 5个
  - 多用户数据混淆
  - 金币并发竞态条件
  - 连抽金币重复扣除
  - 补卡卡片无限使用
  - 金币可以变成负数

- 🟠 High（高危）: 3个
  - 密码明文存储
  - 缺少数据库事务保护
  - 数据库迁移破坏性降级

- 🟡 Medium（中危）: 7个
  - 会话管理不完善
  - 转盘权重可篡改
  - 缺少输入验证
  - 内测码硬编码
  - 习惯打卡金币漏洞
  - Flow 异常未捕获
  - 用户会话永不过期
  - 转盘历史无限增长

- 🟢 Low（低危）: 5个
  - 缺少日志和审计
  - 状态管理可优化
  - 未实现的功能
  - 日期解析不一致
  - 代码中的 TODO

### 按影响范围分类
- 数据安全: 8个
- 金币系统: 5个
- 用户认证: 3个
- 性能优化: 2个
- 用户体验: 2个

---

## 🎯 最紧急修复清单

### 今天必须修复（影响核心功能）
1. ✅ 连抽金币重复扣除 - 直接影响用户金币
2. ✅ 补卡卡片无限使用 - 破坏游戏平衡
3. ✅ 金币可以变成负数 - 数据完整性

### 本周必须修复（安全风险）
4. 多用户数据混淆 - 最严重的架构问题
5. 金币并发竞态条件 - 可能导致金币异常
6. 数据库迁移破坏性降级 - 可能丢失所有数据

### 两周内修复（安全加固）
7. 密码明文存储
8. 缺少数据库事务保护
9. 习惯打卡金币漏洞

---

## 🛡️ 安全加固建议

### 1. 添加全局异常处理
```kotlin
class FunLifeApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        
        // 设置全局异常处理器
        Thread.setDefaultUncaughtExceptionHandler { thread, throwable ->
            Log.e("FunLife", "Uncaught exception", throwable)
            // 保存崩溃日志
            // 显示友好的错误提示
        }
    }
}
```

### 2. 添加数据验证层
```kotlin
object DataValidator {
    fun validateUsername(username: String): ValidationResult {
        return when {
            username.length < 3 -> ValidationResult.Error("用户名至少3个字符")
            username.length > 20 -> ValidationResult.Error("用户名最多20个字符")
            !username.matches(Regex("^[a-zA-Z0-9_]+$")) -> 
                ValidationResult.Error("用户名只能包含字母、数字和下划线")
            else -> ValidationResult.Success
        }
    }
    
    fun validateCoins(amount: Int): Boolean {
        return amount >= 0 && amount <= 1000000
    }
}
```

### 3. 添加操作日志
```kotlin
@Entity(tableName = "operation_logs")
data class OperationLog(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val userId: Long,
    val operation: String,  // "login", "spin", "purchase", etc.
    val details: String,
    val timestamp: Long = System.currentTimeMillis()
)
```

### 4. 添加数据备份
```kotlin
suspend fun backupDatabase(context: Context): Result<File> {
    return withContext(Dispatchers.IO) {
        try {
            val dbFile = context.getDatabasePath("funlife_database")
            val backupDir = File(context.getExternalFilesDir(null), "backups")
            backupDir.mkdirs()
            
            val backupFile = File(backupDir, "backup_${System.currentTimeMillis()}.db")
            dbFile.copyTo(backupFile, overwrite = true)
            
            Result.success(backupFile)
        } catch (e: Exception) {
            Result.failure(e)
        }
    }
}
```

---

## 📝 总结

本次补充排查又发现了 **10个新漏洞**，加上之前的10个，总共 **20个安全漏洞和设计缺陷**。

最关键的发现：
1. **补卡卡片可以无限使用** - 这是一个严重的游戏逻辑漏洞
2. **金币可以变成负数** - 数据完整性问题
3. **数据库迁移会删除所有数据** - 用户数据安全风险

建议立即组织团队会议，制定详细的修复计划和时间表。

---

## 🔗 相关文档
- 主报告：`安全漏洞排查报告.md`
- 修复进度跟踪：建议创建 GitHub Issues 或 Jira 任务
