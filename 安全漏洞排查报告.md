# 🔒 FunLife 项目安全漏洞排查报告

## 📅 排查日期
2026年3月16日

## 🎯 排查范围
- 数据库设计与数据完整性
- 用户认证与会话管理
- 金币系统与交易安全
- 并发控制与竞态条件
- 数据隔离与多用户支持
- UI状态管理

---

## 🚨 严重漏洞（Critical）

### 1. ⚠️ 多用户数据混淆漏洞
**严重程度**: 🔴 Critical  
**影响范围**: 所有用户数据（习惯、心情、目标、金币、转盘历史等）

**问题描述**:
- 所有数据表（habits, mood_entries, goals, user_coins, spin_wheel_history等）都没有 `userId` 字段
- 不同用户登录后会看到和操作同一份数据
- 用户A的习惯打卡会被用户B看到和修改
- 金币系统是全局共享的（user_coins表只有一条记录，id固定为1）

**受影响的文件**:
```
- data/model/Habit.kt
- data/model/MoodEntry.kt
- data/model/Goal.kt
- data/model/UserCoins.kt
- data/model/SpinWheelHistory.kt
- data/model/Anniversary.kt
- data/dao/HabitDao.kt (所有查询都没有 WHERE userId = ?)
- data/dao/MoodDao.kt
- data/dao/GoalDao.kt
- data/dao/CoinDao.kt
```

**修复建议**:
1. 为所有数据模型添加 `userId: Long` 字段
2. 修改所有DAO查询，添加用户隔离条件
3. 在插入数据时自动关联当前登录用户ID
4. 数据库迁移：添加 userId 列并设置默认值

**示例修复**:
```kotlin
// 修改前
@Query("SELECT * FROM habits WHERE isActive = 1")
fun getAllActiveHabits(): Flow<List<Habit>>

// 修改后
@Query("SELECT * FROM habits WHERE userId = :userId AND isActive = 1")
fun getAllActiveHabits(userId: Long): Flow<List<Habit>>
```

---

### 2. ⚠️ 金币系统并发竞态条件
**严重程度**: 🔴 Critical  
**影响范围**: 金币扣除和增加操作

**问题描述**:
- `CoinRepository.spendCoins()` 存在 TOCTOU (Time-of-Check-Time-of-Use) 漏洞
- 检查余额和扣除金币之间没有原子性保证
- 多次快速点击可能导致金币被多次扣除或负数金币

**问题代码**:
```kotlin
// CoinRepository.kt
suspend fun spendCoins(amount: Int): Boolean {
    val currentCoins = getCoinsAmount()  // ← 检查时刻
    return if (currentCoins >= amount) {
        coinDao.spendCoins(amount)       // ← 使用时刻（中间可能被其他操作修改）
        true
    } else {
        false
    }
}
```

**修复建议**:
1. 使用数据库事务确保原子性
2. 在SQL层面进行余额检查
3. 添加乐观锁或悲观锁机制

**示例修复**:
```kotlin
// CoinDao.kt
@Query("UPDATE user_coins SET coins = coins - :amount WHERE id = 1 AND coins >= :amount")
suspend fun spendCoinsAtomic(amount: Int): Int  // 返回受影响的行数

// CoinRepository.kt
suspend fun spendCoins(amount: Int): Boolean {
    return coinDao.spendCoinsAtomic(amount) > 0
}
```

---

### 3. ⚠️ 连抽模式金币重复扣除
**严重程度**: 🔴 Critical  
**影响范围**: 转盘连抽功能

**问题描述**:
- 在 `EnhancedSpinWheelScreen.kt` 中，连抽模式下金币被扣除了 `multiSpinCount` 次
- 但实际上应该一次性扣除总金额

**问题代码**:
```kotlin
// EnhancedSpinWheelScreen.kt:564
repeat(multiSpinCount) {
    viewModel.checkAndDeductCoins()  // ← 每次都扣除 costPerSpin
}
```

**实际效果**:
- 3连抽：扣除 3 × costPerSpin × 3 = 9倍金币（应该是3倍）
- 10连抽：扣除 10 × costPerSpin × 10 = 100倍金币（应该是10倍）

**修复建议**:
```kotlin
// 修改为一次性扣除
val totalCost = currentMode.costPerSpin * multiSpinCount
if (userCoins >= totalCost) {
    coinRepository.spendCoins(totalCost)  // 只扣除一次
} else {
    snackbarHostState.showSnackbar("❌ 金币不足！")
    return@launch
}
```

---

## ⚠️ 高危漏洞（High）

### 4. 🔐 密码明文存储
**严重程度**: 🟠 High  
**影响范围**: 用户账户安全

**问题描述**:
- 用户密码以明文形式存储在数据库中
- 数据库泄露会直接暴露所有用户密码

**受影响文件**:
```
- data/model/User.kt (password: String)
- repository/UserRepository.kt (直接比对明文密码)
```

**修复建议**:
1. 使用 BCrypt、Argon2 或 PBKDF2 进行密码哈希
2. 添加盐值（salt）增强安全性
3. 注册时哈希密码，登录时比对哈希值

**示例修复**:
```kotlin
// 使用 BCrypt
import org.mindrot.jbcrypt.BCrypt

// 注册时
val hashedPassword = BCrypt.hashpw(password, BCrypt.gensalt())
val user = User(username = username, password = hashedPassword, ...)

// 登录时
val user = userDao.getUserByUsername(username)
if (user != null && BCrypt.checkpw(password, user.password)) {
    // 登录成功
}
```

---

### 5. 🔄 缺少数据库事务保护
**严重程度**: 🟠 High  
**影响范围**: 所有涉及多步操作的业务逻辑

**问题描述**:
- 整个项目中没有使用 `@Transaction` 注解
- 复杂操作（如转盘抽取：扣金币→保存历史→发奖励）可能部分失败
- 数据不一致风险

**修复建议**:
```kotlin
@Dao
interface SpinWheelDao {
    @Transaction
    suspend fun performSpinTransaction(
        coinCost: Int,
        history: SpinWheelHistory,
        coinReward: Int
    ) {
        // 原子性执行：扣除金币、保存历史、发放奖励
        coinDao.spendCoins(coinCost)
        historyDao.insert(history)
        if (coinReward > 0) {
            coinDao.addCoins(coinReward)
        }
    }
}
```

---

## ⚡ 中危漏洞（Medium）

### 6. 📱 会话管理不完善
**严重程度**: 🟡 Medium  
**影响范围**: 用户登录状态

**问题描述**:
- SharedPreferences 存储会话信息，容易被root设备读取
- 没有会话过期机制
- 没有自动登出功能

**修复建议**:
1. 使用 EncryptedSharedPreferences 加密存储
2. 添加会话过期时间（如7天）
3. 添加Token刷新机制

---

### 7. 🎲 转盘权重可被客户端篡改
**严重程度**: 🟡 Medium  
**影响范围**: 转盘公平性

**问题描述**:
- 所有转盘逻辑在客户端执行
- 用户可以通过修改APK改变中奖概率
- 没有服务端验证

**修复建议**:
- 关键抽取逻辑移至服务端
- 客户端只负责展示动画
- 服务端返回抽取结果

---

### 8. 🔢 缺少输入验证
**严重程度**: 🟡 Medium  
**影响范围**: 数据完整性

**问题描述**:
- 用户输入（用户名、昵称、习惯名称等）缺少长度和格式验证
- 可能导致UI显示异常或数据库错误

**修复建议**:
```kotlin
// 添加验证
fun validateUsername(username: String): ValidationResult {
    return when {
        username.length < 3 -> ValidationResult.Error("用户名至少3个字符")
        username.length > 20 -> ValidationResult.Error("用户名最多20个字符")
        !username.matches(Regex("^[a-zA-Z0-9_]+$")) -> 
            ValidationResult.Error("用户名只能包含字母、数字和下划线")
        else -> ValidationResult.Success
    }
}
```

---

## ℹ️ 低危问题（Low）

### 9. 📊 缺少日志和审计
**严重程度**: 🟢 Low  
**影响范围**: 问题追踪和安全审计

**问题描述**:
- 关键操作（登录、金币交易、抽奖）没有日志记录
- 无法追踪异常行为

**修复建议**:
- 添加操作日志表
- 记录关键操作的时间、用户、结果

---

### 10. 🔄 状态管理可优化
**严重程度**: 🟢 Low  
**影响范围**: 用户体验

**问题描述**:
- `hasUserClicked` 等状态管理较为复杂
- 可能存在边缘情况未覆盖

**修复建议**:
- 使用状态机模式管理转盘状态
- 简化状态转换逻辑

---

## 📋 修复优先级建议

### 🔴 立即修复（1-2天）
1. **多用户数据混淆** - 这是最严重的问题，会导致用户数据完全混乱
2. **连抽金币重复扣除** - 直接影响用户体验和金币经济平衡
3. **金币并发竞态条件** - 可能导致金币异常

### 🟠 尽快修复（3-5天）
4. **密码明文存储** - 安全基础设施
5. **数据库事务保护** - 数据一致性保障

### 🟡 计划修复（1-2周）
6. **会话管理** - 提升安全性
7. **转盘权重验证** - 如果需要公平性保证
8. **输入验证** - 提升健壮性

### 🟢 持续优化
9. **日志审计** - 长期运维需求
10. **状态管理** - 代码质量提升

---

## 🛠️ 修复实施计划

### 第一阶段：数据隔离（最高优先级）
1. 创建数据库迁移脚本，为所有表添加 userId 字段
2. 修改所有数据模型，添加 userId 属性
3. 修改所有 DAO 查询，添加用户过滤
4. 修改所有 Repository 和 ViewModel，传递当前用户ID
5. 全面测试多用户场景

### 第二阶段：金币系统加固
1. 修复连抽金币扣除逻辑
2. 实现原子性金币操作
3. 添加事务保护
4. 添加金币操作日志

### 第三阶段：安全增强
1. 实现密码哈希
2. 加密会话存储
3. 添加输入验证
4. 实现操作审计

---

## 📝 总结

本次排查发现了 **10个安全漏洞和设计缺陷**，其中：
- 🔴 严重漏洞：3个
- 🟠 高危漏洞：2个
- 🟡 中危漏洞：3个
- 🟢 低危问题：2个

**最关键的问题是多用户数据混淆**，这会导致：
- 用户A的数据被用户B看到和修改
- 金币系统全局共享
- 无法实现真正的多用户支持

建议立即着手修复严重漏洞，确保应用的基本可用性和数据安全。

---

## 📞 联系方式
如需详细的修复代码示例或技术支持，请随时联系。
