// AvatarFrameConfigGenerated.kt - 自动生成的头像框配置
// ⚠️ 此文件由 analyze_avatar_frames.py 自动生成，请勿手动编辑
package com.example.funlife.data.model

/**
 * 自动生成的头像框配置初始化器
 * 
 * 使用方法：
 * 在 AvatarFrameConfigManager.init() 中调用 initializeGeneratedConfigs()
 */
object AvatarFrameConfigGeneratedInitializer {
    
    fun initializeGeneratedConfigs() {
        
        // xiangkuang/A4头像框 - 20个文件
        AvatarFrameConfigManager.addConfigForSeries(
            seriesPath = "xiangkuang/A4头像框",
            originalWidth = 1100,
            originalHeight = 1100,
            transparentAreaRatio = 0.63f,  // 🔥 调整为63%，填满非正圆的内部区域
            offsetX = 0.0f,
            offsetY = 0.0f
        )
        
        // xiangkuang/头像框1 - 123个文件
        AvatarFrameConfigManager.addConfigForSeries(
            seriesPath = "xiangkuang/头像框1",
            originalWidth = 606,
            originalHeight = 605,
            transparentAreaRatio = 0.63f,  // 🔥 调整为63%，填满非正圆的内部区域
            offsetX = 0.0f,
            offsetY = 0.0f
        )
        
        // xiangkuang/头像框2 - 100个文件
        AvatarFrameConfigManager.addConfigForSeries(
            seriesPath = "xiangkuang/头像框2",
            originalWidth = 476,
            originalHeight = 476,
            transparentAreaRatio = 0.63f,  // 🔥 调整为63%，填满非正圆的内部区域
            offsetX = 0.0f,
            offsetY = 0.0f
        )
        
        // xiangkuang/头像框4 - 135个文件
        AvatarFrameConfigManager.addConfigForSeries(
            seriesPath = "xiangkuang/头像框4",
            originalWidth = 233,
            originalHeight = 233,
            transparentAreaRatio = 0.63f,  // 🔥 调整为63%，填满非正圆的内部区域
            offsetX = 0.0f,
            offsetY = 0.0f
        )
        
        // xiangkuang/头像框5 - 20个文件
        AvatarFrameConfigManager.addConfigForSeries(
            seriesPath = "xiangkuang/头像框5",
            originalWidth = 1100,
            originalHeight = 1100,
            transparentAreaRatio = 0.63f,  // 🔥 调整为63%，填满非正圆的内部区域
            offsetX = 0.0f,
            offsetY = 0.0f
        )
        
        // xiangkuang/头像框6 - 209个文件
        AvatarFrameConfigManager.addConfigForSeries(
            seriesPath = "xiangkuang/头像框6",
            originalWidth = 497,
            originalHeight = 507,
            transparentAreaRatio = 0.63f,  // 🔥 调整为63%，填满非正圆的内部区域
            offsetX = 0.0f,
            offsetY = 0.0f
        )
        
        // xiangkuang/头像框7 - 115个文件
        AvatarFrameConfigManager.addConfigForSeries(
            seriesPath = "xiangkuang/头像框7",
            originalWidth = 312,
            originalHeight = 348,
            transparentAreaRatio = 0.63f,  // 🔥 调整为63%，填满非正圆的内部区域
            offsetX = 0.0f,
            offsetY = 0.0f
        )
        
        // xiangkuang/头像框8 - 156个文件
        AvatarFrameConfigManager.addConfigForSeries(
            seriesPath = "xiangkuang/头像框8",
            originalWidth = 400,
            originalHeight = 400,
            transparentAreaRatio = 0.63f,  // 🔥 调整为63%，填满非正圆的内部区域
            offsetX = 0.0f,
            offsetY = 0.0f
        )
    }
}
