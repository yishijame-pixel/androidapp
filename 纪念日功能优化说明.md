# 纪念日功能优化说明

## 本次优化内容

### 1. 剩余天数背景透明度调整 ✅
- **优化**: 将"还有多少天"的背景透明度从 0.95 降低到 0.5
- **效果**: 背景更透明，更好地展示背景图片
- **文字颜色**: 改为纯白色，确保在透明背景上清晰可读

**修改位置**: `AnniversaryCard.kt`
```kotlin
// 之前
Color.White.copy(alpha = 0.95f)

// 现在
Color.White.copy(alpha = 0.5f)
```

### 2. 添加纪念日按钮可全屏拖动 ✅
- **功能**: 添加纪念日的浮动按钮现在可以在整个屏幕范围内自由拖动
- **拖动范围**: 
  - 横向：可以从右边拖到左边
  - 纵向：可以从顶部拖到底部，包括底部导航栏区域
- **实现方式**: 使用 `pointerInput` 和 `detectDragGestures` 实现拖动手势

**技术实现**:
```kotlin
// 拖动状态
var fabOffsetX by remember { mutableStateOf(0f) }
var fabOffsetY by remember { mutableStateOf(0f) }

// 拖动手势
.pointerInput(Unit) {
    detectDragGestures { change, dragAmount ->
        change.consume()
        fabOffsetX = (fabOffsetX + dragAmount.x).coerceIn(
            -(screenWidthPx - 200f), // 左边界
            0f // 右边界
        )
        fabOffsetY = (fabOffsetY + dragAmount.y).coerceIn(
            -(screenHeightPx - 200f), // 上边界
            100f // 下边界，允许拖到底部导航栏
        )
    }
}
```

## 使用说明

### 拖动添加按钮
1. 长按"添加纪念日"按钮
2. 保持按住状态，拖动到屏幕任意位置
3. 松开手指，按钮会停留在新位置
4. 按钮位置会在当前会话中保持，关闭应用后会重置到默认位置（右下角）

### 查看纪念日卡片
- 背景图片现在更加突出，透明度更低的"剩余天数"背景让图片更清晰可见
- 文字使用纯白色，在半透明背景上更易读

## 技术细节

### 布局结构
```
Box (全屏容器)
├── Scaffold (主要内容)
│   └── Column (纪念日列表)
└── ExtendedFloatingActionButton (可拖动的添加按钮)
```

### 拖动边界计算
- **左边界**: `-(screenWidthPx - 200f)` - 确保按钮不会完全移出屏幕左侧
- **右边界**: `0f` - 默认位置（右对齐）
- **上边界**: `-(screenHeightPx - 200f)` - 确保按钮不会完全移出屏幕顶部
- **下边界**: `100f` - 允许按钮拖到底部导航栏下方

## 注意事项

1. **按钮位置**: 拖动后的位置不会永久保存，重新打开应用会恢复到默认位置
2. **拖动手势**: 需要长按并拖动，单击仍然是打开添加对话框
3. **透明度**: 如果觉得 0.5 的透明度太低，可以调整为 0.6 或 0.7

## 后续可优化项

1. **保存按钮位置**: 可以将拖动后的位置保存到 SharedPreferences，下次打开时恢复
2. **吸附效果**: 可以添加边缘吸附效果，松手时自动吸附到最近的边缘
3. **动画效果**: 可以添加拖动时的缩放或阴影动画
4. **多种透明度**: 可以在设置中让用户自定义透明度级别
